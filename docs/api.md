# API

OpenAPI **gerado do código** pelo `platform` (1 operacoes em 1 paths) em `docs/openapi.json` — paths e métodos são reais, esquemas de corpo não são inferidos. Swagger UI na aba **API** do [Backstage](https://backstage.home.lab/catalog/default/api/app-queue-advisor-pricing-api).

| Método | Path | Resumo |
|---|---|---|
| `POST` | `/api/compare` | POST /api/compare |
